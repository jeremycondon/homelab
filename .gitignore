# Local config — never commit
.env
.sops.yaml

# Decrypted secrets — never commit plaintext
secrets/*.env
secrets/*.yml
!secrets/*.example
!secrets/*.enc

# Data volumes
/data/

# OS
.DS_Store
*.swp
*~

