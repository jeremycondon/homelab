server {
    listen 80;
    server_name pihole.pinet.local;

    location / {
        proxy_pass http://localhost/;
        proxy_set_header Host localhost;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
